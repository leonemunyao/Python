#include "dynamic.h"
#include <stdio.h>

void main()
{
fun();
}

// Then compile the driver program. The next step is to link the compiled driver program to the static librar. Note that L. is used
// to tell that the static library is in the current folder 